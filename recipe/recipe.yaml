context:
  name: bumps
  version: "1.0.4"

package:
  name: ${{ name|lower }}
  version: ${{ version }}

source:
  url: https://pypi.org/packages/source/${{ name[0] }}/${{ name }}/${{ name }}-${{ version }}.tar.gz
  sha256: cb85fd1b93859ecbe7eefefba7399c51f56d9ff3a23e6a74128c30683271ff89

build:
  number: 0
  noarch: python
  script: ${{ PYTHON }} -m pip install . --no-deps --ignore-installed -vvv
  python:
    entry_points:
      - bumps = bumps.webview.server.cli:main

requirements:
  host:
    - python ${{ python_min }}.*
    - pip
    - setuptools
  run:
    - python >=${{ python_min }}
    - numpy
    - scipy
    - h5py
    - dill
    - matplotlib-base
    - blinker
    - aiohttp
    - python-socketio
    - plotly
    - mpld3
    - cloudpickle
    - msgpack-python
    - uncertainties

tests:
  - python:
      imports:
        - bumps
        - bumps.dream
        - bumps.gui
        - bumps.mystic
        - bumps.mystic.examples
        - bumps.mystic.optimizer
  - files:
      source:
        - pyproject.toml
    requirements:
      run:
        - python ${{ python_min }}.*
        - pytest
        - pytest-cov
    script:
      - pytest --pyargs bumps

about:
  license: CC-PDDC
  license_file: LICENSE.txt
  summary: Data fitting with bayesian uncertainty analysis
  homepage: https://github.com/bumps/bumps
  repository: https://github.com/bumps/bumps
  documentation: https://bumps.readthedocs.io/en/latest/

extra:
  recipe-maintainers:
    - tschoonj
    - bmaranville
    - mrakitin
